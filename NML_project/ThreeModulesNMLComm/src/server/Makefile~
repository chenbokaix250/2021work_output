# Makefile automatically generated by rcsdesign on
# Wed Jul 03 00:22:05 CST 2013 for /home/hutb/Program/UGVAuto/src/main
# This makefile should define the sources, headers and objects
# stored or created with this directory and then include
# the application include Makefile.

ifndef DEVP_BIN_DIR
DEVP_BIN_DIR =../../bin
endif

ifndef DEVP_LIB_DIR 
DEVP_LIB_DIR =../../lib
endif

ifndef RCSLIB_DIR
RCSLIB_DIR=/usr/local/rcslib
endif

CXXFLAGS:=$(CXXFLAGS) -m64 -pipe -O2 -W -fPIE $(DEFINES) -I$(RCSLIB_DIR)/include -I$(RCSLIB_DIR)/plat/linux/include -I../message

LIBS:=-L$(RCSLIB_DIR)/lib -L$(RCSLIB_DIR)/plat/linux/lib -L/usr/local/lib/ -lrcs -lpthread $(DEVP_LIB_DIR)/libTest.a

ifndef JAVA
JAVA=java
endif

ifndef RANLIB
RANLIB=ranlib
endif

ifndef AR
AR=ar
endif

ifndef CXX
CXX=g++
endif

ifndef NML_CODEGEN
NML_CODEGEN=$(JAVA) -jar /usr/local/bin/CodeGenCmdLine.jar 
endif

SRCS =  \
	MapPositionsvr.cc \
#	ServerAsvr.cc	\


HEADERS = \

OBJS = \
	MapPositionsvr.o \
#	ServerAsvr.o	\



BINS = \
	$(DEVP_BIN_DIR)/MapPositionsvr \
#	$(DEVP_BIN_DIR)/ServerAsvr \




# Rules for specific binaries

all: $(BINS)

.PHONY: all

$(DEVP_BIN_DIR)/%svr: %svr.o
	$(CXX) $^ $(CXXFLAGS) $(LIBS) -o $@

clean:
	-\rm -f *.o *.exe $(DEVP_BIN_DIR)/*svr

.PHONY: clean


